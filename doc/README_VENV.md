# 🛠 프로젝트 시작 및 가상환경 설정 가이드

이 프로젝트(`ppt-study-assistant`)를 다시 열었을 때, 터미널 프롬프트 앞에 **`(venv)`**가 없다면 아래 순서대로 입력하세요.

## 1. 가상환경 활성화 (가장 중요)
사용 중인 운영체제(iMac/Mac)에 맞는 명령어를 터미널에 입력합니다.

* **명령어:** `source venv/bin/activate`
* **성공 확인:** 터미널 프롬프트 앞에 **`(venv)`** 표시가 생겼는지 확인하세요.

---

## 2. 필수 라이브러리 관리
새로운 라이브러리를 추가하거나 환경을 맞출 때 사용합니다.

* **설치:** `pip install -r requirements.txt`
* **목록 저장:** `pip freeze > requirements.txt`

---

## 3. 환경 변수(API Key) 세션 등록
회사 API 키를 현재 터미널에 즉시 적용합니다.

* **명령어:** `export $(grep -v '^#' .env | xargs)`
* **확인:** `echo $ANTHROPIC_API_KEY`

---

## 4. 서비스 실행
* **Streamlit:** `streamlit run app.py`
* **Claude Code:** `claude`

---

## 💡 팁
1. **VS Code 자동 설정:** `Cmd+Shift+P` -> `Python: Select Interpreter` -> `venv` 선택.
2. **종료:** `deactivate` 입력.
